services:
  - type: web
    name: biosprout-api
    runtime: python
    buildCommand: echo "deps will install at start"
    startCommand: pip install -r requirements.txt && uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
